% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_reconstruction_module.R
\name{shape_reconstruction_ui}
\alias{shape_reconstruction_ui}
\alias{shape_reconstruction_server}
\title{Shape Reconstruction Module}
\usage{
shape_reconstruction_ui(id)

shape_reconstruction_server(id)
}
\arguments{
\item{id}{Module ID}
}
\description{
Interactive Shiny module for reconstructing shapes from PCA models.
Loads reconstruction models created by \code{\link{shape_analysis}} and generates
shape outlines from user-specified PC scores.
}
\details{
This module provides an interactive interface for:
\itemize{
  \item Loading reconstruction models (.rds files)
  \item Viewing model information and variance explained
  \item Setting PC scores interactively with sliders
  \item Reconstructing and visualizing individual shapes
  \item Batch reconstruction along PC axes for morphospace exploration
  \item Downloading coordinates and plots
}

The reconstruction uses the eigenvectors and EFA coefficients stored in the
model to transform PC scores back into outline coordinates via inverse Fourier
transformation.
}
\section{UI Features}{
\describe{
  \item{Load Model}{File picker for selecting reconstruction model RDS files}
  \item{Model Info}{Display of model parameters and variance explained}
  \item{PC Score Inputs}{Interactive numeric inputs for each PC (up to 10)}
  \item{Reconstruction}{Single shape reconstruction with customizable plot size}
  \item{Batch Mode}{Generate multiple shapes along a PC axis}
  \item{Downloads}{Export coordinates (CSV) and plots (PNG)}
}
}
\section{Batch Reconstruction}{
The batch mode generates a series of shapes along a selected PC axis:
\itemize{
  \item Choose which PC to vary
  \item Set minimum and maximum values (in SD units)
  \item Specify number of steps
  \item Option to hold other PCs at zero or current values
  \item Displays all shapes in a grid layout
}
}
\examples{
\dontrun{
# In a Shiny app
library(shiny)

ui <- fluidPage(
  shape_reconstruction_ui("recon")
)

server <- function(input, output, session) {
  shape_reconstruction_server("recon")
}

shinyApp(ui, server)

# Typical workflow:
# 1. Run shape_analysis() to create reconstruction model
# 2. Load the *_reconstruction_model.rds file in this module
# 3. Adjust PC scores to explore morphospace
# 4. Download reconstructed shapes
}
}
\seealso{
\code{\link{shape_analysis}} for creating reconstruction models

\code{\link{load_reconstruction_model}} for loading models programmatically
}
